import * as React from 'react';
import Layout from '../../components/layout';

export interface CommandProps {
    
}
 
const Command: React.SFC<CommandProps> = ({}) => {
    return ( 
        <Layout>
            <h1>Command Managment</h1>
        </Layout>
     );
}
 
export default Command;