import * as React from 'react';
import Layout from '../../components/layout';

export interface DispatchProps {}

const Dispatch: React.SFC<DispatchProps> = ({}) => {
  return (
    <Layout>
      <h1>Dispatch Bitch</h1>
    </Layout>
  );
};

export default Dispatch;
